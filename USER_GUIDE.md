# MyLifeOS Windows 使用指南

MyLifeOS 是一款本地优先的桌面效率工具。习惯、任务、专注记录和设置默认只保存在当前 Windows 电脑上，不依赖网络账户。

## 启动与退出

- 安装版：从桌面或开始菜单启动 MyLifeOS。
- 源码版：在 PowerShell 中执行
  `powershell -ExecutionPolicy Bypass -File .\start-local.ps1`。如需手工使用 npm，先运行
  `npm run build`，再运行 `npm run prod`。
- 如需桌面快捷方式，可先执行
  `powershell -ExecutionPolicy Bypass -File .\create-desktop-shortcut.ps1`，之后使用生成的 MyLifeOS 快捷方式。
- 默认点击窗口关闭按钮时，应用会隐藏到系统托盘，计时器仍可继续运行。
- 单击托盘图标或选择“显示窗口”可恢复界面；选择“退出 MyLifeOS”才会完全退出。
- 可在独立的“设置”入口的“桌面端”区域关闭“最小化到托盘”。

源码版首次启动需要 Node.js 22.12.0 或更高版本。启动脚本会在需要时安装依赖和构建前端，并在每次启动前重新编译 Electron 主进程，避免运行拉取或切换分支前留下的旧代码。

## 常用快捷键

- `Ctrl+1`：切换到计划页面
- `Ctrl+2`：切换到画像页面
- `Ctrl+K`：打开任务搜索
- `N`：在计划页面新建临时任务；输入框或弹窗获得焦点时不会触发
- `Escape`：关闭当前弹窗或番茄钟覆盖层

## 数据保存位置

Windows 安装版和源码桌面版通常把数据保存在：

```text
%APPDATA%\MyLifeOS\
```

在文件资源管理器地址栏粘贴上述路径即可打开。主要文件如下：

- `app-data.json`：习惯、目标、每日任务、语言和用户设置
- `recovery-points.json`：自动恢复点和操作前恢复点

桌面版写入这些 JSON 文件时，会先写同目录临时文件，确认落盘后再整体替换；旧的完整版本保存在同名 `.bak` 文件中。若主文件在异常退出后无法解析，应用会自动尝试读取 `.bak`。正常写入期间，强制结束进程可能丢失 300 毫秒合并窗口内的最新操作；若保存已失败，还可能丢失尚未重试或导出的内存修改。原子替换保护上一次已保存的完整 JSON。
- `pomodoro-state.json`：正在进行的番茄钟及待处理恢复信息
- `window-state.json`：窗口位置、大小和最大化状态

请不要在应用运行时手工编辑这些文件。排查问题前，应先退出应用并复制整个 `MyLifeOS` 文件夹作为备份。

## 手动备份与恢复

### 0.1.3 计划执行

- 顶部“搜索任务”支持名称、备注和复盘文字检索，也可按日期范围、目标、优先级和状态筛选；点击结果会跳到任务日期。
- 未完成的临时任务可用卡片上的日历按钮改期。任务进入目标日期待办池，保留编号、备注和复盘；运行中的计时器任务不能改期。
- 删除当日任务后，页面下方会显示 10 秒“撤销删除”。如果原排期时段已有其他任务，撤销会把任务恢复到待办池。
- 习惯规则可以设为每天、工作日或自选星期；日期范围与星期同时生效。旧规则默认每天，修改规则不会重写历史记录。
- “习惯配置”和“设置”有独立入口；备份、恢复和恢复点位于“设置 → 数据管理”。窄窗口下可切换待办池与时间线。

0.1.3 导出的备份格式为 **schema v6**，保留习惯的星期规则。0.1.2 不支持导入 v6；升级前请保留原有备份。0.1.3 仍可导入有效的旧版备份，旧习惯按每天解释。

### 0.1.2 保存状态与异常保护

- 窗口顶部显示“保存中 / 已保存 / 保存失败”；“已保存”表示数据文件已完成写入。
- 保存失败时，应用显示上次已落盘的数据并暂停编辑和计时器。未保存的修改保留在内存，可选择“重试保存”或“导出待保存数据”。重试成功后计时器保持暂停，由你手动继续。
- 正常退出会尝试保存；失败时可返回应用导出、重试或明确放弃后退出。强制结束进程或断电仍会丢失内存中尚未落盘的修改。
- 主数据文件和 `.bak` 都不可读时，应用进入恢复页面，停止普通写入。选择有效备份并确认后，会先将损坏文件复制为同目录的 `.corrupt-<唯一标识>` 文件，再恢复数据；复制失败则不会继续覆盖。
- 如果恢复页面提示仍有计时器，请先选择“停止当前计时器”，再确认恢复。

0.1.2 导出的备份格式为 schema v5，包含目标、习惯、每日任务，以及语言、专注、计划、画像、桌面五组设置。窗口位置、自动恢复点列表和运行中的计时器不在普通备份中。导入旧格式时保留当前设置；不支持的未来格式会被拒绝。

恢复预览显示目标、习惯、日期、任务、设置数量及过滤记录数。导入通过一次完整快照替换提交，实际落盘后才显示成功。从恢复点还原也需要先保存当前数据备份。新备份不应导入 0.1.1 等不支持 v5 的旧版本。

1. 打开“设置”。
2. 在“本地数据管理”中选择“备份数据”。
3. Windows 保存对话框会让你选择 `.json` 文件的保存位置；MyLifeOS 不会擅自上传备份。
4. 恢复时选择“恢复数据”，再选择先前导出的 JSON 文件。
5. 确认恢复后，MyLifeOS 会先要求保存一份当前数据备份。只有该备份实际保存成功后，导入才会继续；取消或保存失败都会停止恢复。

建议把手动备份保存到项目目录以外的位置，例如个人备份盘或受信任的同步文件夹。

## 自动恢复点

MyLifeOS 最多保留 7 个恢复点，按最新优先显示：

- 当天第一次数据变更时创建每日自动恢复点
- 导入备份前创建操作前恢复点
- 从恢复点还原前再创建一个保护恢复点

可在“设置 → 数据管理”的“恢复点”区域查看最近记录并还原。恢复点保存在本机的 `recovery-points.json`，它不能替代存放在其他磁盘上的手动备份。

## 番茄钟恢复

番茄钟由 Electron 主进程计时。暂停、关闭窗口或重启应用后，活动会话可从 `pomodoro-state.json` 恢复；离线期间已经到期的会话会在下次启动时询问如何处理。

## 常见问题

### 源码启动脚本执行后没有窗口

先检查项目根目录的 `start-local.log`。常见原因包括未安装 Node.js、依赖安装失败或 Electron 运行时尚未下载完成。

### 点击关闭后任务栏没有窗口

这是默认的“最小化到托盘”行为。请检查 Windows 通知区域中的 MyLifeOS 图标，或在设置中关闭该选项。

### 导入前取消了备份保存

数据导入会被安全取消，当前数据不会被替换。重新执行恢复并完成操作前备份即可。

### 可以直接复制数据目录迁移吗

可以，但必须先完全退出 MyLifeOS。更推荐使用应用内的 JSON 备份和恢复功能，因为它会检查数据版本和无效记录。
